<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gamerxadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">
        <activity android:name=".ui.useroperation.transactions.UserTransactions"></activity>
        <activity android:name="com.example.gamerxadmin.ui.match.players.JoinedPlayersActivity" />
        <activity android:name="com.example.gamerxadmin.ui.match.UpcomingMatches" />
        <activity android:name="com.example.gamerxadmin.ui.match.CreateMatch" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.GoogleSignIn" />
    </application>

</manifest>